RSpec.describe Json::Schema::Serializer do
  it "has a version number" do
    expect(Json::Schema::Serializer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
